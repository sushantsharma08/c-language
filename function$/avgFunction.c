#include <stdio.h>

int avg();
int main()	
{	
    int a,b,c;
    printf("Enter the value of a, b and c :");
    scanf(" %D %d %d",&a,&b,&c);
     avg()    
    
    return 0;
}
int avg(){
       printf(" the avg value is : %d",(a+b+c)/3);
}